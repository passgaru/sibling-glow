# Agent notes

> [!IMPORTANT]
> Avoid rewriting published git history — force pushing, or rebasing/amending/
> squashing commits that are already pushed. Keep the connected branch in a
> working state.

Content lives in `src/data/rakhiData.ts`. Routes are file-based under
`src/routes`. Design tokens are in `src/styles.css`.
